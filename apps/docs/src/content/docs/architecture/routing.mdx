---
title: Routing
description: The app/[locale] route tree, covering marketing and app route groups, API routes, the .well-known catch-all and the error surfaces.
sidebar:
  order: 2
---

All user-facing routes live under `apps/web/src/app/[locale]/`. The locale segment is driven by `next-intl` with `localePrefix: 'as-needed'` (`apps/web/src/infrastructure/i18n/routing.ts`): the default locale (English) has no URL prefix, every other locale from `apps/web/src/infrastructure/i18n/locales.ts` gets one (`/es`, `/ca`, `/it`, `/fr`, `/de`).

## Route tree

```text
src/app/
├── [locale]/
│   ├── layout.tsx              # root layout: fonts, providers, analytics
│   ├── error.tsx               # locale-scoped error boundary
│   ├── not-found.tsx           # locale-scoped 404
│   ├── (marketing)/
│   │   ├── page.tsx            # homepage
│   │   ├── layout.tsx
│   │   ├── error.tsx
│   │   └── legal/              # cookie-policy, legal-notice,
│   │                           # privacy-policy, terms-of-service
│   └── (app)/
│       ├── planner/            # the tool (sidebar layout)
│       └── payment/confirmation/
├── api/                        # route handlers (unlocalised)
├── .well-known/[...slug]/      # agent-facing discovery documents
├── global-error.tsx            # root error boundary (own <html> shell)
├── global-not-found.tsx        # root 404 (experimental.globalNotFound)
├── robots.ts / sitemap.ts / favicon.ico / fonts.ts
```

## Route groups

- **`(marketing)`** holds the public homepage (`apps/web/src/app/[locale]/(marketing)/page.tsx`, composed from sections in `apps/web/src/ui/modules/pages/homepage/sections/`) and the four legal pages, each with a colocated `metadata.ts`. The group has its own `layout.tsx` and `error.tsx`.
- **`(app)`** holds the product itself. `planner/page.tsx` renders the planner; `planner/layout.tsx` wraps it in the sidebar shell (`SidebarProvider`, `AppSidebar`, store initialisation, premium modal, donate widget, footer). `payment/confirmation/page.tsx` reads the `payment_intent` search param and shows the Stripe payment result; requests without that param never reach it, because the [proxy](/architecture/middleware/) redirects them home.

Route groups affect layouts only, not URLs: `/planner` and `/payment/confirmation` have no group segment in the path.

## API routes

API routes live outside `[locale]` and are excluded from the proxy matcher (except `/api/markdown`).

| Route | File | Purpose |
| --- | --- | --- |
| `POST /api/payment` | `apps/web/src/app/api/payment/route.ts` | Creates a Stripe PaymentIntent (rate-limited, Effect-based) |
| `GET /api/payment/activate` | `apps/web/src/app/api/payment/activate/route.ts` | Stripe's `return_url`. Activates Premium and redirects to the confirmation page with the cookie already set |
| `GET/POST /api/check-session` | `apps/web/src/app/api/check-session/route.ts` | Verifies / mints the premium JWT cookie |
| `POST /api/contact` | `apps/web/src/app/api/contact/route.ts` | Contact form → Resend email + Turso record |
| `POST /api/webhooks/stripe` | `apps/web/src/app/api/webhooks/stripe/route.ts` | Stripe webhook (payment succeeded/failed fallback) |
| `GET /api/markdown` | `apps/web/src/app/api/markdown/route.ts` | Markdown rendition of a listed page. Only reachable, and only drivable, through the proxy rewrite |
| `GET /api/health` | `apps/web/src/app/api/health/route.ts` | Health check |

`GET /api/payment/activate` is the one that grants an entitlement, so it carries four guards: the Stripe client secret is required and compared against the retrieved intent's own in constant time, `redirect_status` short-circuits before Stripe is called at all, the request is rate-limited on `cf-connecting-ip`, and the redirect is explicitly `no-store` because it carries a `Set-Cookie`. Failure redirects with `activation=failed` rather than letting the confirmation page claim Premium is active.

## The `.well-known` catch-all

`apps/web/src/app/.well-known/[...slug]/route.ts` is a lookup table, not a router. It joins the slug segments and asks `lookupWellKnownDocument` (`apps/web/src/infrastructure/well-known/documents.ts`) for one of three exact keys (`api-catalog`, `mcp/server-card.json` and `agent-skills/index.json`), and anything else is a 404. That table is a `Map`: as a plain object it answered `/.well-known/constructor` with an inherited `Object.prototype` function, which passed the presence guard and was then called as a handler.

**The three document modules return plain objects; the route owns the envelope.** Each builder takes the base URL from the Cloudflare environment (`getCloudflareContext`) and returns a value; the route serialises it and sets the per-document `Content-Type` and the shared `WELL_KNOWN_CACHE_CONTROL` (`apps/web/src/infrastructure/well-known/slugs.ts`). A fourth document is a fourth entry in the map and nothing else. Together with the markdown rewrite in the proxy, this is the app's agent-facing surface.

## Error and not-found surfaces

There are per-locale boundaries (`apps/web/src/app/[locale]/error.tsx`, `apps/web/src/app/[locale]/not-found.tsx`) plus root-level `apps/web/src/app/global-error.tsx` and `apps/web/src/app/global-not-found.tsx`. The latter requires `experimental.globalNotFound: true` in `next.config.ts` and handles URLs that never match the `[locale]` segment. All four render the shared brutal designs; see [error pages](/architecture/error-pages/) for the full breakdown.

## Everything else at the app root

- `apps/web/src/app/robots.ts` and `apps/web/src/app/sitemap.ts` generate `robots.txt` and the sitemap.
- `apps/web/src/app/fonts.ts` declares the four `next/font` families whose CSS variables the root layout applies.
- `apps/web/src/app/[locale]/layout.tsx` is the composition root for providers: intl client provider, theme, lazy motion, skeleton bones, cookie consent, WebMCP and analytics.
