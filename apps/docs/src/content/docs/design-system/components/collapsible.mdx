---
title: Collapsible
description: Single fold-out region with a brutal default trigger and a height/opacity spring.
sidebar:
  order: 7
  badge:
    text: Live
    variant: success
---

import { COLLAPSIBLE_CONTENT_PROP_ROWS, COLLAPSIBLE_PROP_ROWS, COLLAPSIBLE_TRIGGER_PROP_ROWS, CollapsibleAsChildDemo, CollapsibleDemo } from '../../../../components/demos/CollapsibleDemo';
import { PropsTable } from '../../../../components/PropsTable';

Source: `apps/web/src/ui/modules/core/animate/base/Collapsible.tsx`. Base UI collapsible with a Motion-animated panel; requires a `LazyMotionProvider` ancestor. Where [Accordion](/design-system/components/accordion/) manages a list of exclusive folds, Collapsible is a single independent one.

Exports: `Collapsible`, `CollapsibleTrigger`, `CollapsibleContent`.

<CollapsibleDemo client:visible />

## How it works

- `CollapsibleContent` animates `height: 0 ↔ 'auto'` plus opacity with a spring (default `stiffness: 150, damping: 22`, `transition` prop to override). The panel is `keepMounted` and rendered through Base UI's `render` callback, which the component uses to strip DOM drag handlers that would collide with Motion's gesture props.
- The default `CollapsibleTrigger` is a full-width brutal button: 3px frame border, `shadow-brutal-btn`, hover lift, and, the distinctive bit, `aria-expanded` keeps the trigger in the lifted "pressed" style while the panel is open, so open state reads at a glance.
- `CollapsibleTrigger asChild` swaps in your own element via Base UI's `render`, skipping the default styling entirely.

<CollapsibleAsChildDemo client:visible />

## Props

`Collapsible` forwards Base UI's root props; the ones that matter:

<PropsTable rows={COLLAPSIBLE_PROP_ROWS} />

`CollapsibleTrigger` adds one on top of Base UI's trigger:

<PropsTable rows={COLLAPSIBLE_TRIGGER_PROP_ROWS} />

`CollapsibleContent` adds one on top of Base UI's panel:

<PropsTable rows={COLLAPSIBLE_CONTENT_PROP_ROWS} />

## Usage

```tsx
import { Collapsible, CollapsibleContent, CollapsibleTrigger } from '@ui/modules/core/animate/base/Collapsible';

<Collapsible defaultOpen>
  <CollapsibleTrigger>Advanced options</CollapsibleTrigger>
  <CollapsibleContent>…</CollapsibleContent>
</Collapsible>
```

Controlled mode works through the Base UI root props (`open`, `onOpenChange`); the root mirrors them into context for the animation.

## Conventions

- `data-slot` hooks: `collapsible`, `collapsible-trigger`, `collapsible-content`.
- The default trigger uses `hit-area-stable`, matching [Button](/design-system/components/button/) hover mechanics.

## Accessibility

- The trigger carries `aria-expanded` and `aria-controls` from Base UI, and the panel sets `aria-hidden` while closed so its content leaves the accessibility tree.
- Enter and Space toggle; there is no arrow-key navigation, because a Collapsible is a single fold rather than a set.
- With `asChild`, the child element receives the aria attributes; keep it a button or an element with `role='button'`.

## In the app

The sidebar's grouped steps (`SidebarCollapsibleGroup.tsx`), the Strategy explainer in `Strategy.tsx`, the advanced options of the Donation form (`DonationForm.tsx`) and the weekend rows of the Holiday table (`HolidaysTable.tsx`).
