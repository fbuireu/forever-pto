---
title: Docs CI
description: How this documentation site builds, previews and deploys itself, deliberately simpler than the app's pipeline.
sidebar:
  order: 7
---

This page is self-documenting: it describes the pipeline that published it. The site is a static Astro/Starlight build served as Worker assets (`forever-pto-docs`, configured in `apps/docs/wrangler.toml`), so its CI can be far simpler than the [app's](/infra/workflows/): no runtime secrets, no database, no teardown.

## The workflow

`.github/workflows/docs.yml` follows the repo's workflow conventions (SHA-pinned actions, `persist-credentials: false`, zizmor-linted). It runs on every push and pull request, and a `changes` job gates `build` on the `DOCS_PATHS` list: `apps/docs/**`, `apps/web/package.json`, all of `apps/web/src/ui/**`, `apps/web/src/domain/calendar/types.ts`, `apps/web/src/infrastructure/i18n/locales.ts`, `pnpm-lock.yaml`, the workflow file and `.github/actions/**`. Unrelated app changes never build the docs; changes to the reused sources do, on purpose, because they can break the demos. The list used to be a `paths:` trigger filter, written out twice, and a filtered workflow never reports on a pull request outside its paths, which is why nothing in it could be a required check.

The last three app entries are not decoration. `app-version.ts` reads the app manifest, and `HowItWorksDemo.tsx` reaches `@domain/calendar/types` and `@infrastructure/i18n/locales` by relative path rather than through `@ui`. The app's own contract suite derives that reach from the sources and fails if a path of it is missing from `DOCS_PATHS`, so this list cannot drift from what the site imports.

Triggers:

- `pull_request` → `changes`, then build & test + versioned preview when the range touches the list, and always `Check (docs)`, the aggregate the ruleset requires.
- `push` to `main` → build & test + production deploy + smoke run + release when the range touches the list.
- `workflow_dispatch` → treated as a docs change: build, deploy and smoke, which is how `ci.yml`'s `docs-refresh` rebuilds the site after a web release and how a redeploy without a commit is done.

## Build & test job

Installs the whole workspace (**unfiltered** `pnpm install`: the demos import app sources whose bare imports resolve from the root `node_modules`), then runs, via the scripts in `apps/docs/package.json`:

- `pnpm typecheck`: `astro check`, which typechecks the demo islands. This is the drift tripwire: demos import real app components via the `@ui` alias with exhaustive `Record<Variant, …>` maps, so renaming a CVA variant in the app breaks this build.
- `pnpm build`: `astro build`; the resulting `apps/docs/dist` is uploaded as an artifact for the deploy jobs.
- Playwright (`pnpm test:e2e`) runs against a local preview server: `apps/docs/playwright.config.ts` boots `astro preview` itself via `webServer`, so no deployment is needed to test. Seven specs are site-level smoke tests; the rest walk **every** page the build emitted a demo onto, asserting each answers 200, that every `data-demo` frame renders something once scrolled into view, and that the page raises no `pageerror` and logs no `console.error`. That last assertion is the one that matters: a demo can throw during hydration and still build green.

There is no Biome step here. Both packages are linted in one root pass from the app workflow; a second one over `apps/docs` would duplicate it.

## PR previews: one Worker per pull request

Both packages preview the same way. The preview job downloads the build artifact the `build` job already produced and deploys it as its own Worker:

```bash
wrangler deploy --env development --name pr-<n>-forever-pto-docs-development
```

The `--name` override is what mints a fresh Worker rather than updating the stable one, so two open pull requests never overwrite each other's preview. It resolves at:

```text
https://pr-<n>-forever-pto-docs-development.fbuireu.workers.dev
```

The `development` environment carries no custom domain, so a preview cannot answer on docs.forever-pto.com however it is deployed. Every push to the PR redeploys the same name, so the link the job posts once stays valid for the PR's whole life.

What the docs preview does *not* need is what separates it from the app's: no bindings, no `--secrets-file` on the deploy, no `--var` override, and no second build; it ships the `docs-dist` artifact.

### Teardown

`cleanup-development.yml` deletes the Worker when the pull request closes, in a `cleanup-docs` job beside the app's. A per-PR Worker that nothing tears down accumulates forever.

The app's job in that workflow has to queue behind the CI run, because app E2E drives the per-PR Worker over the network and deleting it mid-run turns every remaining spec into a placeholder. The two jobs carry a concurrency group each: `cleanup-web` on `CI-…`, `cleanup-docs` on `docs-…`, the group of the workflow that deployed the Worker each one deletes. They shared one workflow-level group until 2026-08-28, and it was `ci.yml`'s, so `cleanup-docs` waited on a run that has nothing to do with its Worker while the `preview` job that does own it could still be mid-deploy. The docs Playwright suite genuinely needs no wait, since it runs against the build artifact in the `build` job before the preview Worker exists at all; the deploy it must not race is the preview deploy itself.

## Production deploy

On `main`, the deploy job reuses the build job's `apps/docs/dist` artifact and runs `wrangler deploy` via `wrangler-action` (the `deploy` script in `apps/docs/package.json` is the local one-shot, though it passes no `--env`, so it deploys the top-level config to the production Worker's name without its route block), activating a new version of `forever-pto-docs` on `docs.forever-pto.com` (an infrastructure-as-code `custom_domain` route; see [Cloudflare stack](/infra/cloudflare/)). The only credentials involved are `CLOUDFLARE_API_TOKEN` and `CLOUDFLARE_ACCOUNT_ID`; the site has no [runtime secrets](/infra/secrets/) at all.

## Accepted cost: docs PRs still run app CI

`ci.yml` opens with a `changes` job that derives whether `apps/web` was touched, and gates `deploy-production`, `release-web` and `deploy-development` on it, so a docs-only PR no longer deploys an app preview Worker or runs app E2E. Lint, typecheck and unit tests stay unconditional, because the documentation contract suite reads `CONTEXT.md`, `adr/` and every guide and must not be skipped by a markdown-only change.

The obvious fix, `paths-ignore: ['apps/docs/**']` on the app workflows, has a branch-protection caveat: required status checks that never run **never report**, and GitHub blocks the merge waiting for them. That is why neither workflow carries a path filter: each has a `changes` job that gates its own jobs with `if:`, and an aggregate job that always reports, `Check` in `ci.yml` and `Check (docs)` in `docs.yml`, is what the ruleset requires. The redundancy that remains, the contract suite running on every pull request, is deliberate.

Two related conventions for docs PRs:

- Title them `docs:` or `chore:`; a `feat:`/`fix:` docs PR would [cut an app release](/infra/release/).
- Docs dependencies live in `apps/docs/package.json` but share the root `pnpm-lock.yaml`, so [Renovate](/infra/renovate/) manages them under the same policy.
